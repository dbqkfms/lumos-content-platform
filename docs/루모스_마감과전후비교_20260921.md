# 루모스 사이트 최종 마감과 전후 비교 — 2026-09-21

## 현재 결과

영상 작업이 아닌 사이트 마감이다. 기존 누적44파일에9파일을 추가 보강하여 중복 제외48파일 선택 수정본을 전달했다. 이번9파일은 App, RouteNavigation(신규), ErrorBoundary, NotFound, BackToTop, BackgroundMedia, PreviewPlayer, siteRefinement.css, Footer다. 오류 상세 비노출·한국어404/복구·경로 포커스·뒤로가기 보존·늦은 미디어 이벤트·버퍼링·사업자정보 TBD 노출을 다뤘다. 문의 입력란/동의, 개인정보/약관/회사정보 원본, 작품 축/카탈로그, 기존 영상 바이트는 보존했다.

**이 브랜치의 활성 client를 그48파일로 바꾼 것은 아니다. 실제 코드는 대화의 아래 ZIP에 있다. 이번 Git 갱신은 인계 기록이다.** 원격 구버전 client를 운영 사이트에 통째로 배포하지 않는다.

## 실제 전달물

- 루모스_사이트_전후비교.html — 28,652,121바이트, SHA256 `761704adf918151c5724f5fd765b106d613b4a9bd3c5e7586615689185dceaa6`
- 루모스_최종마감_코드와검증.zip — 496,106바이트, SHA256 `aae0f9e3611a8cb7946deb96e7e88e428dbe71629bc8be43ffcf82156a62ab3b`
- 루모스_별도배포용_비교사이트.zip — 20,408,876바이트, SHA256 `7e1b9bed190d7677f1b810d4118bf4eaac0380120b5894b74a5cd4197661c6ef`
- 루모스_최종확인과배포상태.md — SHA256 `c5d97f111115f309ad9ac2063538686ed3595fa04f8dd0867d23c3bd1e48967a`

대화/Library에서 해당 이름과 해시로 확인한다. 다음 환경에 동일 sandbox 경로가 존재한다고 가정하지 않는다.

전후 비교본은 동일 페이지에서 백업 복구 화면과 최종 화면을 전환하며, 기존 운영사이트는 별도 탭 링크다. 수정 전은 현재 운영 배포와 동일한 SHA의 확정 스냅샷이 아니다. 실제 문의/계정 등록은 차단하고 입력은 임시 메모리다. 이미지67개를 내장하고 영상은 원래 서버 주소를 사용한다. 별도배포 ZIP은 이미지 분리형 정적 비교자료이며 새 프로젝트로만 올린다. 기존 .vercel/project.json, 키, 고객기록, 폰트파일은 없다.

## 이번 최종 코드의 검증

전체 TypeScript와 전체 빌드(Vite·서버번들·14경로 메타) 통과. 기존컴포넌트19/19, 출시보강19/19, 새마감15/15, 최종전후HTML19/19, 모의SMTP14/14, 적용복원12/12, 메타안전8/8, 보존/패치13/13. 합계119개 세부 사례이지119개 실제 브라우저 테스트가 아니다. 별도배포형HTML도 같은19개 검사를 재실행했으며 중복 합산하지 않는다. 이전108/198/313개 결과를 더하지 않는다. 전달용 재생성 도구의 HTML도 SHA까지 동일했다.

JSDOM에서 기하·미디어·통신은 모의 값이다. 최신CSS 파서 미지원13알림과 의도적인 오류시연2건을 분리했고 예상 밖 JS 오류는0이었다. 브라우저 탐색은 ERR_BLOCKED_BY_ADMINISTRATOR였으며 우회하지 않았다. 실제 화면 캡처/실기/영상 디코딩/시각 상업품질 인증은 하지 않았다. 영상6개·이미지67개는 공급ZIP과 바이트가 같다.

## 배포와 출시

Vercel 팀0개, 대상프로젝트조회403(scope gys537359-9731s-projects), deploy_to_vercel 도구는 JSON-RPC -32602 Tool not found였다. **새 온라인 URL 생성 실패, 운영 사이트/main/Drive 원본 덮어쓰기0건.** 별도 정적 배포자료를 실제 배포 완료로 부르지 않는다. 다른 계정·토큰/정책 우회는 하지 않는다.

현재 권한에서 독립 수행 가능한 사이트 마감·기능검사·원본보존·비교본 전달은 종료한다. 같은 자료 재수집/카피/검사보조도구만 반복하지 않는다. 남은 것은 허용된 실제 브라우저 검수, 배포 원본과 정확한 잠금파일 설치, 실제 인증·서버권한·문의수신, 사업자정보·작품권리·개인정보 처리 근거다. esbuild 선언^0.25.12/실행0.25.10 불일치는 남아 있어 새 설치부터 재현했다고 표현하지 않는다. 실제 인증 차단은 실제 인증 구현이 아니다.

## 이어가기

ZIP 00_먼저읽기 → 해시 check → 별도 작업복사본 선택 적용 → 정확 설치·같은코드검사·실브라우저 → 새 프리뷰 배포. 기존 운영주소를 새 버전으로 연결하지 않는다. 최초대조원본용 누적48patch와 직전44본용 추가9patch는 중복 적용하지 않는다. 원본/추가 편집이 다르면 적용·복원 거부. 사용자 실제 운영 승인 전 production 반영하지 않는다. 영상 작업으로 돌아가지 않는다.
