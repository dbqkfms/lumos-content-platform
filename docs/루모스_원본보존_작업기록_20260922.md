# 루모스 원본보존 작업 기록 — 2026-09-22

## 이 브랜치의 정확한 범위

`work/lumos-site-edit-20260922`의 기준은 GitHub `main`의 `30c8316d929d58b56f9127bf1da4f6a9ea6cf159`이다. 해당 Git 트리에서 분기한 선택 수정이며, 현재 온라인 사이트의 전체 소스를 확정 복구한 판본이라고 표시하지 않는다.

배포 메타데이터의 Git SHA만으로 CLI 업로드 파일 전체의 동일성을 증명할 수 없다. 기존 R4 복구 후보, 비교용 HTML, GitHub 기본 브랜치, 실제 배포본은 서로 구분한다. 기존 디자인을 다시 만드는 작업이나 R4 비교 HTML로 서비스를 대체하는 작업을 수행하지 않는다.

## 실제 소스 수정

- 공개 목록 요청은 응답 본문 수신까지 15초로 제한한다. HTTP 실패, HTML 대체 응답, 잘못된 JSON, 8MiB 초과, 10,000개 초과 배열은 종료한다. 타이머·구독·스트림을 정리한다. 기존과 같은 same-origin 인증 쿠키 처리를 유지하고, 재시도·POST·업로드는 추가하지 않는다.
- 잘못된 개별 항목은 제외하되 정상 이웃 항목은 유지한다. Content Manager의 원래 배열 인덱스를 보존하여 기존 Open 샘플 분류 기준이 밀리지 않게 한다.
- 작성된 한글 제목·언어별 제목·설명·태그를 우선 보존한다. 없는 값에만 종전 대체값을 쓴다. 명시적인 빈 태그 배열을 덮어쓰지 않는다.
- 이미 사라진 화면의 목록 구독에는 상태를 다시 쓰지 않는다. 정적 작품 우선 중복 제거와 오류 시 정적 목록 유지 동작은 그대로 둔다.

## 보존 및 한계

문의 양식·입력항목·동의, 인증·포털 코드, 라우트, 로고·CSS·이미지·영상·원본 데이터, 패키지 선언과 잠금파일, Vercel 설정을 수정하지 않는다. 작품 ID·분류·공개 단계·영상 주소가 같은지는 기준 코드와 회귀 비교한다. 이는 기존 분류를 상업 사용 승인으로 인정한다는 뜻이 아니다.

새 테스트는 단위 함수와 모의 React 구독 검사다. 실제 브라우저·실계정 로그인·실문의 수신 검증으로 표현하지 않는다. GitHub Actions는 이 작업 브랜치에서만 읽기 권한으로 설치·타입·회귀·빌드를 실행하며, 배포 명령·배포 토큰·main 병합을 포함하지 않는다. 결과는 해당 커밋의 실행 로그를 확인한 뒤 판정한다.

## 완료 기준과 다음 단계

1. 허용한 파일만 변경되었고 기준 브랜치가 보존되었는지 확인한다.
2. 기존 잠금파일의 새 설치, 전체 타입검사, 회귀검사, 전체 빌드의 실제 종료 코드를 확인한다.
3. 실제 배포 업로드와 일치하는 소스·미디어를 확정한 뒤, 그 판본에 이 변경을 선택 이식하고 별도 Preview에서 검증한다. 이미 같은 기능이 구현된 R4 파일에 이 오래된 기준 파일을 통째로 덮어쓰지 않는다.
4. 기존 사이트와 main은 별도 승인 전 교체하지 않는다. 실제 문의 수신·인증과 작품 권리·운영사 정보는 별도 운영 확인 항목이다.

## 복원

이 작업은 main/기존 배포를 수정하지 않는다. 작업 브랜치에서 변경을 철회할 때는 이 작업의 커밋을 확인하고 `git revert <변경 커밋>`으로 역변경을 만든다. 뒤따르는 작업이 있으면 충돌을 먼저 검토한다. `reset --hard`, 강제 push, 기존 배포의 롤백·삭제를 자동 실행하지 않는다.
