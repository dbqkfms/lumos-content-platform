# 루모스 사이트 수정 — 2026-09-21 전달 지점

## 최우선 사용자 범위

**영상 제작/재편집을 이어가지 않는다. 작업 대상은 사이트다.** 홈·공간 제안·소개·공통 메뉴·한국어 타이포그래피·문의 흐름을 실제 소스에서 수정했다. 같은 자료가 없다고 다시 요청하거나 기존 영상 ZIP으로 돌아가지 않는다.

## 실제 결과와 저장 위치의 구분

대화에 제공한 `루모스_사이트_수정코드와검증.zip` 안에 실제 변경 19파일, 대조 원본, 선택 패치, 해시 확인 후 적용/복원 도구, 재검사 실행기와 증거가 있다. `루모스_사이트_수정검토본.html`은 같은 수정 소스에서 생성한 검토용 HTML이며 실제 문의 전송을 차단한다. `루모스_사이트_수정결과.md`에 변경 내용과 한계를 상세 기록했다.

**이 브랜치의 활성 client/가 그 19파일로 바뀐 것은 아니다. 이 커밋은 인수인계 기록이다.** main 병합·운영 배포·기존 Drive 원본 갱신·실제 문의 전송은 하지 않았다. 기존 Git의 구버전 client를 현재 운영 사이트에 통째로 올리지 않는다.

- 코드 ZIP: 281250 bytes, SHA-256 `72a75f57d4382431b21e2ffb4a3b92507719d92a67833922c6f684e326a1cfce`
- 검토 HTML: 27710562 bytes, SHA-256 `743979481781e9c941d804da22538c55207d62c57537da582d6f8cc8f57e8b5d`
- 수정 결과 문서: SHA-256 `9ade263f159bbb573d3e19c2e2bf7c85373ad7aae2f83963a303c2c7f9489275`
- 검증 작업 트리 지문: `cfab5fcbb382f1d0705276c475b649edc0ce20db6c5916e35349fd327f2e3991`

파일은 대화/Library에서 정확한 이름으로 찾는다. sandbox 경로가 다음 환경에도 존재한다고 가정하지 않는다.

## 이번 코드에서 실행한 검사

전체 TypeScript 5.6.3 검사와 프런트엔드 Vite 빌드 통과. 실제 컴포넌트 JSDOM 19/19, 최종 HTML JSDOM 10/10, 소스·입력·카탈로그 보존 27/27, 적용/복원 안전 12/12 통과. 전달용 재검사 실행기로 19+10을 다시 통과했고 선택 patch의 실제 적용/역적용과 원문 복원도 확인했다. 서로 다른 시점의 이전 198/313개 검사는 합산하지 않는다.

JSDOM에는 실제 픽셀 배치/영상 디코딩 엔진이 없다. 기하·미디어·화면폭·통신을 시험값으로 대체했다. 최종 HTML JS 실행 오류 0, CSS 파서의 최신 구문 미지원 알림 1개를 별도 기록했다. 브라우저 탐색은 ERR_BLOCKED_BY_ADMINISTRATOR로 차단되어 실제 PC/모바일 시각 통과로 표시하지 않았다. 제한을 바꾸거나 우회하지 않았다.

확보된 영상 6개와 이미지 67개는 원문 바이트가 유지된다. 영상 재편집·재인코딩은 없다. 새 영상 전수 디코딩이나 이용 권리 검증을 완료했다고 주장하지 않는다.

## 남은 중요한 조건

- 이후 Drive 개발 파일은 이미 확보했지만 복구 트리에는 이전 Git 파일도 일부 섞여 있다. 실제 운영 배포의 SHA/미커밋 상태 일치는 미확정이다.
- 복구본에 scripts/prerender.mjs가 없고 esbuild 선언은 ^0.25.12, 사용한 실행 환경은 0.25.10이다. 설치부터 전체 운영 빌드까지 재현 성공했다고 주장하지 않는다. 잠금파일은 임의 변경하지 않았다.
- 실제 브라우저에서 390/768/1440px·확대·메뉴/문의·이미지 오류·배경 제어를 확인해야 한다.
- Open/Creator 기존 추정 배지와 사용 권리/검수 기록, 서버 인증, 개인정보 문구 적합성, 실제 문의 수신은 별도 확인 대상이다.

## 이어가기

ZIP의 00_먼저읽기.md → 원본확인_적용복원.py check → 실제 개발 폴더의 별도 작업 트리에 선택 적용 → 실제 잠금파일 설치·전체 빌드·허용된 실제 브라우저 검수 → 현재 배포 연결 확인 순서로 진행한다. 원본·추가 사용자 편집이 다르면 덮어쓰기를 거부하며 롤백은 적용 당시 백업이나 해당 커밋 revert로만 한다.

Open/Free·Originals·Creator Works, STANDARD/LOCAL, public+portal, 문의 8항목+동의2개, 개인정보·약관, 작가/관리자 접근 흐름을 보존한다. 명시적 운영 승인 전 main/운영 반영은 하지 않는다.
