# LUMOS 작업 이어가기

## 최신 시작점 — 2026-09-21 사이트 수정본 전달

**먼저 `docs/루모스_사이트수정_전달완료_20260921.md`를 읽는다. 사용자 요청은 영상 제작이 아닌 웹사이트 수정이다. 영상 재편집·새 나레이션 제작으로 돌아가지 않는다.**

실제 소스 19파일과 검토 HTML을 대화 첨부로 전달했다. 홈·공간 제안·소개·공통 메뉴·한국어 서체와 여백·문의 동작을 수정했다. 정확한 파일명·해시·실행 검사·현재 한계는 위 문서와 대화의 `루모스_사이트_수정코드와검증.zip`, `루모스_사이트_수정결과.md`에 있다.

이 GitHub 브랜치의 활성 client/에는 해당 19파일을 적용하지 않았다. 이번 원격 변경은 인계 기록과 검사용 의존성 준비 워크플로이며, 운영 배포와 main 병합이 아니다. 기존 구버전 client를 현재 배포에 통째로 올리지 않는다.

## 이미 확인한 원본

찾던 이후 개발 소스 후보는 연결된 Google Drive에 있다.

`/Google Drive/20260430 - 컴퓨터 자료 옮기기/Lumos - 드라이브 복사용/Lumos 안티그래비티/lumos-platform-v2`

BuyerProofSummary.tsx, PreviewPlayer.tsx, PrivacyPage.tsx, 10항목 FloatingCTA, App의 포털 보호 코드와 `.vercel/project.json`을 확보했다. projectName은 lumos-v2-preview다. 이미 발견한 파일을 없다고 판단하거나 사용자에게 같은 원본 ZIP을 다시 요청하지 않는다. 복구 작업 트리에 일부 이전 Git 파일도 있으므로 현재 배포 SHA/미커밋 상태까지 동일하다고 간주하지 않는다.

## 실제 검사와 한계

이번 작업 트리의 전체 TypeScript 5.6.3 검사, 프런트엔드 빌드, 실제 컴포넌트 DOM19/19, 검토HTML DOM10/10, 원본·입력·카탈로그 보존27/27, 적용/복원12/12, patch 실제 적용·역적용이 통과했다. 영상6개·이미지67개는 원본 바이트를 유지했다. 이전198/313개 검사와 합산하지 않는다.

DOM 검사는 모의 기하·화면폭·미디어·통신을 사용하므로 실제 브라우저 픽셀 검사와 다르다. ERR_BLOCKED_BY_ADMINISTRATOR 제한을 바꾸거나 우회하지 않았다. 실제 PC/모바일 화면·영상 재생 검수는 남는다.

복구본에 scripts/prerender.mjs가 없고 esbuild 선언 ^0.25.12와 확보 실행 환경0.25.10이 다르다. 잠금파일 설치부터 전체 운영 빌드까지 성공했다고 표현하지 않는다. 운영 배포·실제 문의·서버 보안 검증은 수행하지 않았다.

## 유지 조건

- LUMOS는 B2B-first 미디어아트 플랫폼이다. 단순 제품/영업 사이트로 축소하지 않는다.
- Open/Free, LUMOS Originals, Creator Works 및 STANDARD/LOCAL, public+portal 통합을 유지한다.
- 문의8항목+필수동의2개, 개인정보·약관, 작가/관리자 포털 로그인 흐름을 보존한다. 기능 보존은 법률·서버보안 인증이 아니다.
- 근거 없는 공개 승인·사용 권리·검수·리사이즈·납기·성과 수치를 만들지 않는다. 기존 Open/Creator 추정 배지와 권리 기록 연결은 후속 확인 대상이다.
- 현재 배포 원본 확인과 명시적 운영 승인 전 main 변경·병합·운영 배포·실제 문의 전송을 하지 않는다.
- .env·키·쿠키·고객기록·폰트파일을 산출물에 넣지 않는다.
- 작업을 이미 했다는 기록보다 실제 파일·해시·실행 결과를 우선한다. 실패·미실행·범위 밖 검증을 분리한다.

## 다음 작업

첨부 ZIP의 00_먼저읽기.md와 변경명세.json → 원본확인_적용복원.py check → 별도 작업 트리에 안전한 선택 적용 → 실제 잠금파일 설치·전체 빌드·허용된 브라우저 검수 → 배포 연결 확인 순서다. 카피와 시각 변경을 버리고 검사 보조 도구만 반복해서 만들지 않는다.

다른 해시나 추가 사용자 편집은 덮어쓰지 않는다. 기존6파일 선택 패치는 도구가 인식하므로 중복 적용하지 않는다. 롤백은 적용 당시 백업이나 해당 커밋만 revert하며 hard reset·강제푸시·원본 삭제는 금지한다.

이전 이력은 docs/루모스_영상개정과원본복구_20260920.md, docs/루모스_이식안전검증_20260920.md, docs/루모스_후속검증_20260920.md에 보존한다. 이전 원본 미발견·영상 제작 지시는 현재 다음 작업이 아니다.
