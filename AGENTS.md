# LUMOS 작업 이어가기

## 최신 상태 — 2026-09-20 원본 발견과 영상 개정

먼저 `docs/루모스_영상개정과원본복구_20260920.md`를 읽는다. 그다음 실제 Git/Drive/Vercel 상태를 확인한다. 아래 최신 정정은 이전 인수인계보다 우선한다.

**찾던 이후 개발 소스 후보가 연결된 Google Drive에 있다.**

`/Google Drive/20260430 - 컴퓨터 자료 옮기기/Lumos - 드라이브 복사용/Lumos 안티그래비티/lumos-platform-v2`

이 폴더의 BuyerProofSummary.tsx, PreviewPlayer.tsx, PrivacyPage.tsx, 10항목 FloatingCTA, App의 포털 보호 코드와 `.vercel/project.json`을 실제로 읽었다. projectName은 `lumos-v2-preview`다. 이전 문서의 ‘필수 원본 ZIP이 없다’는 결론은 폐기한다. 이미 발견한 파일을 다시 없다고 말하거나 같은 자료를 사용자에게 재요청하지 않는다. 다만 백업 작업 파일과 현재 배포의 정확한 SHA 일치는 아직 미확정이다.

새로 받은 ZIP7개는 사이트 소스가 아닌 소개영상 재편집 자료다. 자료의 최신 지시에 따라 v9 원고28구간·자막56개·편집표·XML과 실제256.8초 자막+BGM 검토 영상을 만들었다. 새 내레이션 미녹음, 청정 베이스854×480 확대본이므로 최종 HD 마스터로 부르지 않는다. SIGNAL FAIL클립은 사용하지 않았다.

## 사이트 변경의 정확한 범위

실제 후속 소스 후보의 기존3파일+보조3파일 선택 패치는 대화의 `루모스_개정원고_사이트선택패치_검증_20260920.zip`에 있다. **이 GitHub 브랜치의 활성 client 파일에 적용된 것은 아니다.** 이번 저장소 갱신은 작업 기록이다. 이전 PR의 구버전 문의4항목이나 공개277개 화면을 현재 배포에 통째로 이식하지 않는다.

이번 검증: 순수 함수 strict TypeScript5.8.3/구문/AST/실제CM279행 전후 대조313개 통과, 오프라인 보조 함수 DOM32개 통과, 적용·복원7개 통과. 복구 프로젝트 전체 React 타입연결/설치/빌드/실제 사이트 페이지 검증은 미실행이다. 로컬 HTTP 탐색은 관리자 정책으로 차단됐고 정책을 바꾸지 않았다. 자체 HTML의 오프라인 시험을 실제 사이트 브라우저 검증이라고 주장하지 않는다.

기존198개 테스트/전체 빌드는 `b3d4f80cc2acf83f7403bc391db7ad934286160a`, CI `35497881097`의 이전 결과다. 다른 코드·실행의 증거를 합쳐 성공을 만들지 않는다. 그 실행은 미디어와 이식 종합 검증 실패였다.

## 유지 조건

- LUMOS는 B2B-first 미디어아트 플랫폼이다. 단순 영업·제품 사이트로 축소하지 않는다.
- Open/Free, LUMOS Originals, Creator Works 세 축과 STANDARD/LOCAL, public+portal 통합, 작가·관리자 포털을 유지한다.
- `30c8316d`는 기존 Git 비교 기준이며 현재 배포의 확정 커밋이 아니다.
- 배포 원본 확인과 사용자 승인 전 main 변경·병합·운영 배포·메뉴/포털 삭제·실제 문의 전송을 하지 않는다.
- 공개 범위를 숫자로 맞추거나 권리/검수/리사이즈/납기 승인을 만들지 않는다. Open/Creator 배지와 필터는 통제된 근거 연결 시 함께 수정한다.
- 개인정보·약관·동의2개·포털 로그인 이동을 보존하되 법률/서버보안 적합성 인증으로 표현하지 않는다.
- 실패·미실행·범위 밖 검증을 분리한다. 사용자 제공 새 파일의 로컬 검사와 권한으로 차단된 원격자료 접근은 다르다. 이전 차단 작업을 우회하지 않는다.
- .env·키·쿠키·고객기록·폰트파일을 산출물에 넣지 않는다.

## 다음 작업

사이트: 발견한 Drive 원본 후보의 나머지 작업 파일을 복원하고 실제 배포와 대조 → 별도 작업 브랜치 → 원본 해시가 맞는 선택 수정만 적용 → 전체 설치/타입검사/빌드/미디어/브라우저/포털 회귀 → 명시적 운영 승인.

영상: v9 새 음성 제작 → 실제 발화와 예상 자막 동기 조정 → 청정 고해상도 원본 연결 → 전체 재생/제품/음량/납품 검증. 출력된 자막+BGM 검토본을 새 음성까지 완성된 영상으로 부르지 않는다.

적용 전 기존 작업을 보존한다. 다른 해시면 자동 덮어쓰기를 멈춘다. 패치 중복 적용 금지. 되돌릴 때는 해시 검사 후 원본 복원 또는 해당 커밋만 revert하며 hard reset·강제푸시·원본 삭제는 하지 않는다.

이전 이력은 `docs/루모스_이식안전검증_20260920.md`, `docs/루모스_후속검증_20260920.md`, `docs/심층검증결과_20260920.md`에 보존한다. 이전의 원본 미발견 상태는 현재 결론이 아니다.
