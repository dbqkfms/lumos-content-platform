# LUMOS 작업 이어가기

## 최신 시작점 — 사이트 마감·전후 비교 완료

**먼저 `docs/루모스_마감과전후비교_20260921.md`를 읽는다. 사용자 요청은 웹사이트 수정과 기존 버전을 덮어쓰지 않는 별도 비교다. 영상 편집·나레이션 제작으로 돌아가지 않는다.**

현재권한의 독립적인 소스 마감·기능검사·원본보존·전후 비교본 전달은 마무리했다. 같은 자료 재수집·카피·검사보조도구만 반복하지 않는다. 남은 것은 실제 브라우저/배포권한/운영계정·문의/권리·회사정보의 연결이다.

대화의 `루모스_최종마감_코드와검증.zip`은 누적48파일 선택수정, `루모스_사이트_전후비교.html`은 백업/최종 전환형 검토본, `루모스_별도배포용_비교사이트.zip`은 독립정적자료다. `루모스_최종확인과배포상태.md`와 위 인계 문서에 정확한 해시·검사·한계가 있다. 이전19/44파일 전달본과 중복 덮어쓰지 않는다.

**이 GitHub 브랜치 활성 client에는48파일을 적용하지 않았다. 이번 원격수정은 인계 기록뿐이다.** 구버전Git소스를 운영사이트에 통째로 배포하지 않는다. Vercel대상403·배포도구오류로 새 온라인URL은 없고 기존사이트/main/Drive 덮어쓰기도 없다.

## 이미 확보한 원본

`/Google Drive/20260430 - 컴퓨터 자료 옮기기/Lumos - 드라이브 복사용/Lumos 안티그래비티/lumos-platform-v2`

이후개발파일·정책·문의·포털가드·projectName lumos-v2-preview를 이미 확보했다. scripts/prerender.mjs도 후속 작업에서 확보했다. 필요한 원본이 전혀 없다고 다시 말하지 않는다. 다만 복구트리 일부에 이전Git파일도 있어 현재 운영배포SHA/미커밋상태 일치는 미확정이다.

## 검사 구분

최종타입/전체빌드 통과. 현재소스 실제컴포넌트19+출시보강19+새마감15, 비교HTML19, 모의SMTP14, 적용복원12, 메타8, 보존13=119개 세부사례다. 별도배포HTML의 같은19개 재실행은 중복합산하지 않는다. 이전108/198/313과 합산하지 않는다. 영상6·이미지67바이트 보존, 비교본 재생성SHA일치.

DOM의 기하/미디어/통신은 모의 값이다. 실제브라우저는 ERR_BLOCKED_BY_ADMINISTRATOR였고 우회하지 않았다. 화면/모바일/픽셀/실재생 통과로 발표하지 않는다. esbuild선언^0.25.12와실행0.25.10차이가 있어 정확한 잠금파일의 새설치 재현도 미완료다.

## 보존 조건

- B2B-first 미디어아트 플랫폼, Open/Free·Originals·Creator Works, STANDARD/LOCAL, public+portal을 유지한다. 제품영업사이트로 축소하지 않는다.
- 문의 모달8항목+동의2개, 별도문의7항목+동의2개, 개인정보·약관, 작가/관리자 포털경로를 보존한다. 보존은 법률/보안적합성 인증이 아니다.
- 기존원본·영상·유저추가편집을 보존한다. 영상재편집/새자막/불필요한입력란 추가는 하지 않는다.
- 실인증은 미연결이다. 운영 데모인증차단은 인증기능완료가 아니다. 실제문의수신/작품권리/검수/납기/회사정보를 추정으로 승인하지 않는다.
- 현재 사용자는 별도 비교용 배포는 허용했다. 기존운영도메인 덮어쓰기·main병합·실제문의전송은 허용하지 않았다. 필요한 정식권한이 없으면 우회하지 않는다.
- .env·키·쿠키·고객기록·폰트파일을 산출물에 넣지 않는다. 비교본의 noindex는 비공개보안이 아니다.

## 다음 실제 연결

최종ZIP 00_먼저읽기→원본해시check→별도작업복사본선택적용→정확설치·동일코드검사·허용브라우저검수→새프리뷰. 원본이나 추가편집이 다르면 덮어쓰기거부. 롤백은 적용당시백업/해당커밋revert이며 hard reset·강제푸시·기존원본삭제 금지.

이전문서 docs/루모스_사이트수정_전달완료_20260921.md, docs/루모스_영상개정과원본복구_20260920.md, docs/루모스_이식안전검증_20260920.md는 과거상태 기록이다. 현재 재개지점은 최상단 문서다.
